execution:
  concurrency: 10
  holdFor: 5m
  rampUp: 2m
  scenario: yaml_example
  
scenarios:
  yamlExample:
    retrieveResources: false
    requests: {http://example.com/}

reporting:
  module: {final-stats, console}

settings:
  checkInterval: 5s
  defaultExecutor: jmeter

provisioning: local
